server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finflow_application?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Adi@2552
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: finflow-super-secret-key-must-be-at-least-256-bits-long-for-hs256
